name: 'job-a'

on:
  workflow_call:

jobs:
  job-a:
    name: "Teams notification"
    runs-on: [self-hosted,Linux,X64]
    steps:
    - uses: actions/checkout@v3

    - name: "Run job-c"
      shell: bash
      run: |
        echo "Hello job-c"
